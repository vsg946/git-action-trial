name: Verify Email Step

on:

Triggers the workflow on push or manually from the GitHub UI
push:
branches:
- main
workflow_dispatch:

jobs:
verify_step_name:
runs-on: ubuntu-latest
steps:
# This step must contain your email address in its name to satisfy the requirement.
- name: 24f2001081@ds.study.iitm.ac.in
run: |
echo "GitHub Action Step Name Verification Complete."
echo "Email step name confirmed."
echo "This action will be the most recent action run."
